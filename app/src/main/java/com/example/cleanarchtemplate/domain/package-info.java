/**
 * Representation Domain layer. Provides types of data which processing by app on different layers
 * <p>Hierarchy is represented following template:</p>
 *
 * <ul><p>Use cases package: - defines of use cases class which interact with repository</p>
 *     <li>Entity package - defines of data classes which would be returned by use case</li>
 * </ul>
 * @author EvgenySamarin
 * @since 20190905 v2
 */
package com.example.cleanarchtemplate.domain;